program gdi_svg;

uses
  Forms,
  gsMainForm in 'gsMainForm.pas' {Form1},
  frmDraw in 'frmDraw.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
